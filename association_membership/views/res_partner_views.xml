<?xml version="1.0" ?>
<odoo>
    <record id="view_partner_form_membership" model="ir.ui.view">
        <field name="name">res.partner.form.membership</field>
        <field name="model">res.partner</field>
        <field name="inherit_id" ref="base.view_partner_form"/>
        <field name="arch" type="xml">
            <!-- A plain field in the right-hand group, so it keeps its label. -->
            <field name="category_id" position="after">
                <field
                    name="current_membership_number_display"
                    readonly="1"
                    invisible="not current_membership_number_display"
                />
            </field>
            <xpath expr="//notebook/page[@name='contact_addresses']" position="after">
                <page string="Memberships" groups="association_membership.group_membership_viewer">
                    <div class="oe_button_box" name="membership_partner_button_box">
                        <button name="action_create_membership" string="Create Membership" type="object" class="oe_stat_button" icon="fa-plus" groups="association_membership.group_membership_manager"/>
                    </div>
                    <separator string="Memberships"/>
                    <field name="membership_ids" nolabel="1">
                        <list>
                            <field name="membership_number"/>
                            <field name="product_id"/>
                            <field name="company_id"/>
                            <field name="state"/>
                            <field name="date_start"/>
                            <field name="date_end"/>
                            <field name="invoice_partner_id" optional="hide"/>
                            <field name="date_cancelled" optional="hide"/>
                        </list>
                    </field>
                    <separator string="Periods"/>
                    <field name="membership_period_ids" nolabel="1">
                        <list>
                            <field name="membership_id"/>
                            <field name="membership_year" string="Year" options="{'enable_formatting': false}"/>
                            <field name="billing_status"/>
                            <field name="amount"/>
                            <field name="amount_paid"/>
                            <field name="invoice_id"/>
                            <field name="refund_move_id" optional="hide"/>
                        </list>
                    </field>
                </page>
            </xpath>
        </field>
    </record>
    <record id="view_partner_kanban_membership" model="ir.ui.view">
        <field name="name">res.partner.kanban.membership</field>
        <field name="model">res.partner</field>
        <field name="inherit_id" ref="base.res_partner_kanban_view"/>
        <field name="arch" type="xml">
            <!-- Members are read from the kanban, so the number belongs on the card. -->
            <field name="email" position="before">
                <field
                    name="current_membership_number_display"
                    t-if="record.current_membership_number_display.raw_value"
                    class="text-body fw-bold"
                />
            </field>
        </field>
    </record>
    <record id="action_membership_members" model="ir.actions.act_window">
        <field name="name">Members</field>
        <field name="res_model">res.partner</field>
        <field name="view_mode">kanban,list,form</field>
        <field name="domain">[('membership_ids', '!=', False)]</field>
        <field name="context">{'search_default_filter_current_members': 1}</field>
    </record>
    <record id="view_res_partner_filter_membership" model="ir.ui.view">
        <field name="name">res.partner.search.membership</field>
        <field name="model">res.partner</field>
        <field name="inherit_id" ref="base.view_res_partner_filter"/>
        <field name="arch" type="xml">
            <xpath expr="//search" position="inside">
                <separator/>
                <!-- Same meaning as in the reports (CURRENT_MEMBER_STATES): waiting,
                     active, and cancelled but not ended yet. -->
                <filter string="Current Members" name="filter_current_members" domain="[('membership_ids.state', 'in', ('waiting', 'active', 'cancelled'))]"/>
                <filter string="Active Members" name="filter_active_members" domain="[('membership_ids.state', '=', 'active')]"/>
            </xpath>
        </field>
    </record>
</odoo>
