<?xml version="1.0" encoding="utf-8" ?>
<odoo>
    <record id="view_partner_form_membership_numbers" model="ir.ui.view">
        <field name="name">res.partner.form.membership.numbers</field>
        <field name="model">res.partner</field>
        <field name="priority" eval="5" />
        <field name="inherit_id" ref="base.view_partner_form" />
        <field name="arch" type="xml">
            <field name="vat" position="after">
                <label
                    for="current_membership_number_display"
                    string="Membership"
                    class="o_form_label"
                    invisible="not current_membership_number_display"
                />
                <div class="o_row" invisible="not current_membership_number_display">
                    <field
                        name="current_membership_number_display"
                        nolabel="1"
                        readonly="1"
                        class="text-body"
                    />
                </div>
                <!--
                <field
                    name="all_membership_numbers_display"
                    string="All Memberships"
                    widget="html"
                    readonly="1"
                    invisible="not all_membership_numbers_display"
                />
                -->
            </field>
        </field>
    </record>

    <record id="view_partner_form_membership" model="ir.ui.view">
        <field name="name">res.partner.form.membership</field>
        <field name="model">res.partner</field>
        <field name="inherit_id" ref="base.view_partner_form" />
        <field name="arch" type="xml">
            <xpath expr="//notebook" position="inside">
                <page string="Memberships">
                    <div class="oe_button_box" name="membership_partner_button_box">
                        <button
                            name="action_create_membership"
                            string="Create Membership"
                            type="object"
                            class="oe_stat_button"
                            icon="fa-plus"
                            groups="association_membership.group_membership_manager"
                        />
                    </div>
                    <separator string="Memberships" />
                    <field name="membership_ids" readonly="1" nolabel="1">
                        <list create="0" edit="0" delete="0">
                            <field name="membership_number" />
                            <field name="product_id" />
                            <field name="company_id" />
                            <field name="state" />
                            <field name="date_start" />
                            <field name="date_end" />
                            <field name="amount_override" string="Amount" />
                            <field name="last_billing_status" />
                            <field name="invoice_partner_id" optional="hide" />
                            <field name="date_cancelled" optional="hide" />
                            <field name="active" optional="hide" />
                        </list>
                    </field>
                    <separator string="Contributions" />
                    <field name="membership_contribution_ids" readonly="1" nolabel="1">
                        <list create="0" edit="0" delete="0">
                            <field name="membership_id" />
                            <field name="membership_year_display" />
                            <field name="billing_status" />
                            <field name="amount_expected" />
                            <field name="amount_paid" />
                            <field name="invoice_id" />
                            <field name="refund_move_id" optional="hide" />
                        </list>
                    </field>
                </page>
            </xpath>
        </field>
    </record>

    <record id="view_partner_members_overview_search" model="ir.ui.view">
        <field name="name">res.partner.members.overview.search</field>
        <field name="model">res.partner</field>
        <field name="arch" type="xml">
            <search>
                <field name="name" />
                <field name="membership_company_names" />
                <filter
                    name="current_memberships"
                    string="Waiting + Active"
                    domain="[('membership_ids.state', 'in', ('waiting', 'active'))]"
                />
                <filter
                    name="historical_memberships"
                    string="Historical Only"
                    domain="[('membership_ids.state', '=', 'cancelled')]"
                />
            </search>
        </field>
    </record>

    <record id="view_partner_membership_kanban" model="ir.ui.view">
        <field name="name">res.partner.membership.kanban</field>
        <field name="model">res.partner</field>
        <field name="arch" type="xml">
            <kanban sample="1">
                <field name="avatar_128" />
                <field name="is_company" />
                <field name="active" />
                <field name="current_member_numbers_kanban" />
                <templates>
                    <t t-name="card" class="flex-row">
                        <widget name="web_ribbon" title="Archived" bg_color="text-bg-danger" invisible="active" />
                        <aside class="o_kanban_aside_full">
                            <t t-if="!record.is_company.raw_value">
                                <div class="o_kanban_image_fill position-relative w-100">
                                    <field name="avatar_128" alt="Contact image" class="h-100" widget="image" options="{'img_class': 'object-fit-cover'}" />
                                    <field t-if="record.parent_id.raw_value" name="parent_id" class="position-absolute bottom-0 end-0 w-25 bg-light" widget="image" options="{'preview_image': 'image_128', 'img_class': 'object-fit-contain'}" />
                                </div>
                            </t>
                            <t t-else="">
                                <field name="avatar_128" class="w-100" widget="image" options="{'img_class': 'object-fit-contain w-100 h-100'}" />
                            </t>
                        </aside>
                        <main class="ps-2 ps-md-0">
                            <div class="mb-1">
                                <field name="display_name" class="mb-0 fw-bold fs-5" />
                                <field t-if="record.parent_id.raw_value and !record.function.raw_value" class="text-muted" name="parent_id" />
                                <field t-elif="!record.parent_id.raw_value and record.function.raw_value" class="text-muted" name="function" />
                                <div t-elif="record.parent_id.raw_value and record.function.raw_value" class="text-muted">
                                    <field name="function" /> at <field name="parent_id" />
                                </div>
                            </div>
                            <div t-if="record.city.raw_value or record.country_id.raw_value">
                                <field name="city" />
                                <span t-if="record.city.raw_value and record.country_id.raw_value">, </span>
                                <field name="country_id" />
                            </div>
                            <field name="email" class="text-truncate" />
                            <div
                                t-if="record.current_member_numbers_kanban.value"
                                class="text-body mt-1"
                                style="white-space: pre-line;"
                            >
                                <field name="current_member_numbers_kanban" />
                            </div>
                            <footer><div /></footer>
                        </main>
                    </t>
                </templates>
            </kanban>
        </field>
    </record>
</odoo>
